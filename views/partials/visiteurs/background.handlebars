<section class="generale-background">
     {{!-- Image de fond --}}
    <div class="detail-background">
        <h1>{{ title }}</h1>
        <div class="indication-page">
            <a href="/">Accueil</a>
            <ion-icon name="chevron-forward-outline"></ion-icon>
            <p>{{ title }}</p>
        </div>
    </div>
</section>
